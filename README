Installation

  $ git clone https://github.com/joseph-tohdjojo/dot_files.git ~/.dot_files

Create symlinks:

  $ ln -s ~/.dot_files/bash_profile ~/.bash_profile
  $ ln -s ~/.dot_files/bashrc ~/.bashrc
  $ ln -s ~/.dot_files/git-completion.bash ~/.git-completion.bash
  $ ln -s ~/.dot_files/git-prompt.sh ~/.git-prompt.sh
